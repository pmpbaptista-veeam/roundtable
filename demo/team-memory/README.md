# Demo team memory — export-backups-report (dummy data)

A pre-seeded Roundtable team memory for the demo: agents **ada** (API,
human: pedro.baptista) and **grace** (DB, human: dana.lead), one prior
decision (cursor pagination), and one open non-blocking question in grace's
inbox. All content is fictional dummy data.

Run `../setup.sh` to instantiate it as a live git repo with a
local bare "remote" plus one clone per participant under `/tmp/roundtable-demo/`.
